<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/menu_search"
        android:title="search"
        android:icon="@mipmap/ic_search"
        app:showAsAction="ifRoom"
        />
    <item android:id="@+id/menu_wifi"
        android:title="wifi"
        android:icon="@mipmap/ic_wifi"
        app:showAsAction="ifRoom"
        />
    <item android:id="@+id/menu_menu1"
        android:title="切换list"
        android:icon="@mipmap/ic_menu"
        app:showAsAction="never"
        />
    <item android:id="@+id/menu_menu2"
        android:title="切换grad"
        android:icon="@mipmap/ic_menu"
        app:showAsAction="never"
        />
    <item android:id="@+id/menu_menu3"
        android:title="瀑布流"
        android:icon="@mipmap/ic_menu"
        app:showAsAction="never"
        />
</menu>